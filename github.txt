git init
-------------------
git add -a 
----------------------
git commit -m ["name"]
------------------
git log
-----------------
git statue
----------------------
git branch [""]
----------------------
git checkout[""]
---------
git branch
-----------------------------------------------------
ssh key: 
========
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
-------------------------------------
eval $(ssh-agent -s)
---------------------------
clip < ~/.ssh/id_rsa.pub
==========================================================================
add ssh key to project
---------------------------
ssh-add ~/.ssh/id_rs
clip < ~/.ssh/id_rsa.pub
/**************that’s will copy a ssh key 
and u will go to the sitings and paste it in new ssh key
======================================================================================
git push:
===========
git remote add ["name like origin"]["ssh key"]
git push -u origin master

